# E2E Tests

E2E tests cover multi-agent flows, full round-trips, and long-lived servers.
They may be slower or more flaky-prone than lower-level tests.
See `../CONVENTIONS.md` before adding or moving tests here.
